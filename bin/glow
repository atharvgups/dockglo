#!/usr/bin/env bash
case "$1" in
  a|apply)  ./mac/reorder_and_apply.sh apply ;;
  u|undo)   ./mac/reorder_and_apply.sh undo  ;;
  glow|"")  ./mac/reorder_and_apply.sh apply; sleep 2; ./mac/reorder_and_apply.sh undo ;;
  *) echo "usage: glow [apply|undo|glow]"; exit 1 ;;
esac
